{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "attach",
            "name": "Attach to Rust",
            "pid": "${command:pickProcess}",
            "sourceLanguages": [
                "rust",
                "c"
            ]
        },
    ]
}